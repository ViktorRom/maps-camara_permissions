import {StyleSheet} from 'react-native';
import {COLOR} from '../../constants';

export const styles = StyleSheet.create({
  constainer: {
    flex: 1,
  },
  title: {
    color: COLOR.text,
    textAlign: 'center',
    fontSize: 20,
    marginVertical: 10,
    fontWeight: '800',
  },
});
